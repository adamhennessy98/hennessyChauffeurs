/** Canonical production URL — keep in sync with astro.config.mjs `site`. */
export const SITE_URL = 'https://hennessychauffeurs.com';

/** Business contact email (may use .ie domain independently of site URL). */
export const SITE_EMAIL = 'info@hennessychauffeurs.ie';
